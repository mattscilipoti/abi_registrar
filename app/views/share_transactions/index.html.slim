h1 Share Transactions

h2 Summary 

table
  thead
    tr
      th Item 
      th Quantity
      th Total
  tbody
    tr
      td Shares Purchased
      td =ShareTransaction.purchase.sum(:quantity)
      td =number_to_currency(ShareTransaction.purchase.sum(:cost_total))


h2 Listing 

#item_transactions
  - columns = %i[transaction_type cost_per quantity cost_total transacted_at residency from_residency]
  =render 'layouts/models_table', models: @share_transactions, columns: columns, allow_edit: false
br

ul
  li= link_to "Record a Share Purchase", purchase_new_share_transactions_path
  li= link_to "Record a Share Transfer", transfer_new_share_transactions_path
