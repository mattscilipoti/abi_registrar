= searchbar_tag(Property)

fieldset
  legend
    = render 'layouts/h1', title: "Properties", model_count: @properties.size, model_total: Property.count

  #properties
    - columns = %i[street_address section lot_numbers lot_fees_paid? residents_summary]
    =render 'layouts/models_table', models: @properties, columns: columns, allow_sort: true
br
= link_to "New property", new_property_path
