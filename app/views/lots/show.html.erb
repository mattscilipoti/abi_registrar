<h1>
  <%= "Lot ##{@lot.summary}" %>
</h1>

<%= render @lot %>

<div>
  <%= link_to "Edit this lot", edit_lot_path(@lot) %> |
  <%= link_to "Back to lots", lots_path %>

  <%= button_to "Destroy this lot", @lot, method: :delete %>
</div>
